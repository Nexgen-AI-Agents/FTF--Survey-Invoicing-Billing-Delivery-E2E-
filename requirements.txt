# FTF Agentic AI OS — Python Dependencies
# Install: pip install -r requirements.txt
# Python 3.11+ required

# Anthropic Claude API
anthropic>=0.25.0

# HTTP client — FTF API and FEMA API calls
httpx>=0.27.0

# MySQL driver — FTF stage DB direct connection (Sprint 11)
pymysql>=1.1.0

# Environment variable loading
python-dotenv>=1.0.1

# Excel generation (monthly statements)
openpyxl>=3.1.2

# PDF generation (monthly statements)
reportlab>=4.1.0

# Data validation / typed models
pydantic>=2.7.0

# Scheduling (AR daily loop, monthly trigger)
schedule>=1.2.1

# YAML parsing — workflow validation in test_connections.py
PyYAML>=6.0.1

# HTML parsing — county property appraiser page scraping (A2 Data Collector)
beautifulsoup4>=4.12.0

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0
